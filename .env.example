# Docker Compose Environment Variables
# Copy this file to .env and adjust values as needed

# Database Configuration
DB_USERNAME=resume
DB_PASSWORD=resume_password
DB_DATABASE=resume_db

# API Configuration (used by docker-compose)
PORT=4001
NODE_ENV=production

# Frontend Configuration (build-time)
# VITE_GRAPHQL_URL=/api/graphql  # Production (via nginx proxy)
# VITE_GRAPHQL_URL=http://localhost:4001/graphql  # Development
